<div class="stripe-container">
  <div
    class="stripe w-full text-center px-6 py-2 text-4xl font-black md:px-8 md:py-3 lg:px-10 lg:py-4 md:text-6xl lg:text-8xl"
  >
    KHAIR ALANAM
  </div>
</div>

<style>
  .stripe-container {
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    background: linear-gradient(90deg, #ff00ff, #800080, #0000ff, #ff00ff);
    background-size: 400% 100%;
    animation: stripeAnimation 20s linear infinite;
  }

  .stripe {
    background: linear-gradient(90deg, #ff00ff, #800080, #0000ff, #ff00ff);
    background-size: 400% 100%;
    animation: stripeAnimation 20s linear infinite;
  }

  @keyframes stripeAnimation {
    0% {
      background-position: 0 0;
    }
    100% {
      background-position: 400% 0;
    }
  }
</style>
